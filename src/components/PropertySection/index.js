import React from "react";
import { Container } from "./styled-components";

const PropertySection = ({ title, children }) => {
  return <Container>PropertySection</Container>;
};

export { PropertySection };
